# Absolute base URL of the API in production (no trailing slash), e.g.
#   VITE_API_URL=https://the-resolution-api.onrender.com
# Leave unset for local development — Vite proxies /api to localhost:4000.
VITE_API_URL=
